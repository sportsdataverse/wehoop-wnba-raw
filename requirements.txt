-i https://pypi.org/simple
aniso8601>=6.0.0
attrs>=19.1.0
click>=7.1.1
cycler>=0.10.0
itsdangerous>=1.1.0
Jinja2>=2.11.1
jsonschema>=3.0.1
markupsafe>=1.1.1
six>=1.14.0
Werkzeug>=0.16.0
xgboost>=1.2.0
numpy>=1.19.4
pandas>=1.1.4
# Floor must stay >= the release that carries every symbol python/ imports.
# espn_wnba_player_stats_v3 arrived with the 2026-06-08 codegen redesign; the
# old >=0.0.32 floor let a self-hosted runner sit on 0.0.50 and fail stage 06.
sportsdataverse[all]>=0.0.75
pyreadr>=0.4.7
pyarrow>=11.0.0
tqdm>=4.65.0
# argparse and pathlib are NOT listed here on purpose. Both are stdlib since
# Python 3.4; the PyPI packages of those names are abandoned Python-2 backports,
# and installing `pathlib` shadows the stdlib module and breaks modern builds.